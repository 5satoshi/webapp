
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: lightning-stats-dashboard
  description: A NextJS application providing a dashboard for Lightning Network node statistics, AI-powered insights, and channel management.
  annotations:
    github.com/project-slug: https://github.com/5satoshi/webapp
    backstage.io/techdocs-ref: dir:.
spec:
  type: website
  lifecycle: experimental
  owner: user:default/5satoshi # Replace with your team or user if known
  system: # Optional: specify a system if you have one defined in Backstage
  # dependsOn: [] # Optional: list resource or component dependencies
  # providesApis: [] # Optional: list APIs provided by this component
